(function(n) {
    const r = n.mediaSlideContainerSelector || ".js__pr-media-slide .js__media-preview",
        i = n.imgContainerSelector || ".js__media-item-container",
        u = i + " " + (n.imgSelector || "img[src]"),
        f = document.querySelector(r),
        t = f.querySelectorAll(u);
    !t || !t.length || t.forEach(n => {
        var t = document.createElement("img");
        t.onload = function() {
            window.enableDebugLog && console.log(this.width, this.height);
            var t = this.width / this.height;
            if (t < 1) {
                const t = n.closest(i);
                !t || t.classList.add("vertical")
            }
        };
        t.src = n.src;
        t.complete && t.onload && t.onload()
    })
})(window.mediaSlideConfig || (window.mediaSlideConfig = {}));