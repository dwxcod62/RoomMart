window.FrontEnd || (window.FrontEnd = {});
FrontEnd.ListingHistoryServices = function() {
    function i(i) {
        var r = [],
            u, f;
        return localStorage ? (localStorage.getItem(n) ? (u = localStorage.getItem(n), r = JSON.parse(u)) : localStorage.getItem(t) && (u = localStorage.getItem(t), f = JSON.parse(u), r = f.map(n => ({
            productId: n.productId,
            productHtml: ""
        }))), r.filter(n => n.productId == i).length == 0 && r.unshift({
            productId: i,
            productHtml: ""
        }), r) : r
    }

    function r(t) {
        t = t.filter((n, t) => t > 30);
        localStorage.setItem(n, JSON.stringify(t))
    }
    var t = "listingHistoryInDetailsPage",
        n = "listingHistoryPixiuInLDP";
    this.GetListingHistoryFromCache = i;
    this.SaveListingHistoryIntoCache = r
};