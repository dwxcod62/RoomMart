window.FrontEnd || (window.FrontEnd = {});
FrontEnd.YourLocationServices = function() {
    function r(i, r) {
        return new Promise(function(u, f) {
            fetch(n, {
                method: "POST",
                headers: {
                    Accept: "application/json",
                    "Content-Type": "application/json"
                },
                body: JSON.stringify({
                    type: "getward2",
                    lat: i,
                    lon: r
                })
            }).then(n => n.text()).then(n => n ? JSON.parse(n) : undefined).then(n => {
                n ? u({
                    cityCode: n.CityCode,
                    districtId: n.DistrictId,
                    wardId: n.WardId,
                    streetId: 0,
                    projectId: 0
                }) : t().then(u).catch(n => {
                    f(n)
                })
            })
        })
    }

    function t() {
        return new Promise(function(n, t) {
            try {
                fetch("https://api.ipify.org?format=jsonp&callback=?", {
                    method: "GET",
                    headers: {
                        Accept: "application/json",
                        "Content-Type": "application/json"
                    }
                }).then(n => n.json()).then(function(r) {
                    i(r.ip).then(n, t)
                }, function() {
                    i("").then(n, t)
                })
            } catch (r) {
                t(r)
            }
        })
    }

    function i(t) {
        return new Promise(function(i, r) {
            try {
                fetch(n, {
                    method: "POST",
                    headers: {
                        Accept: "application/json",
                        "Content-Type": "application/json"
                    },
                    body: JSON.stringify({
                        type: "getward3",
                        ip: t
                    })
                }).then(n => n.text()).then(n => n ? JSON.parse(n) : undefined).then(function(n) {
                    i({
                        cityCode: n ? n.CityCode : ""
                    })
                })
            } catch (u) {
                r(u)
            }
        })
    }
    var n = "https://apimap.batdongsan.com.vn/api/map_sync";
    this.GetYourLocation = function() {
        return new Promise(function(n, i) {
            navigator.geolocation ? navigator.geolocation.getCurrentPosition(function(t) {
                return localStorage.setItem("ALLOW_SHARE_LOCATION", "1"), r(t.coords.latitude, t.coords.longitude).then(n).catch(function(n) {
                    n && console.log(n)
                })
            }, function() {
                localStorage.setItem("ALLOW_SHARE_LOCATION", "0");
                i()
            }, {
                enableHighAccuracy: !0,
                timeout: 5e3,
                maximumAge: 0
            }) : t().then(n).catch(function(n) {
                n && console.log(n)
            })
        })
    }
};