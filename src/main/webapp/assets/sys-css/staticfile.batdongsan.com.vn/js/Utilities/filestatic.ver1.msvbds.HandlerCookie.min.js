function setCookie(n, t, i, r, u, f) {
    var e = new Date;
    e.setDate(e.getDate() + i);
    document.cookie = n + "=" + escape(t) + (i == null ? "" : "; expires=" + e.toUTCString()) + (r == null ? "" : "; path=" + r) + (u == null ? "" : "; domain=" + u) + (f == null ? "" : "; secure")
}

function getCookie(n) {
    var i = n + "=",
        t = document.cookie;
    return t.length > 0 && (begin = t.indexOf(i), begin != -1) ? (begin += i.length, end = t.indexOf(";", begin), end == -1 && (end = t.length), unescape(t.substring(begin, end))) : null
}

function eraseCookie(n, t, i) {
    getCookie(n) && (document.cookie = n + "=" + (t ? ";path=" + t : "") + (i ? ";domain=" + i : "") + ";expires=Thu, 01 Jan 1970 00:00:01 GMT")
}

function decodeCookie(n) {
    return new Promise(function(t) {
        cookieStore.get(n).then(n => {
            n.value != decodeURIComponent(n.value) && cookieStore.set({
                domain: n.domain,
                expires: n.expires,
                name: n.name,
                partitioned: n.partitioned,
                path: n.path,
                sameSite: n.sameSite,
                secure: n.secure,
                value: decodeURIComponent(n.value)
            }), t()
        })
    })
}
var SERVERNAME = "SERVERNAME",
    SERVER = getCookie(SERVERNAME);
eraseCookie(SERVERNAME);
eraseCookie(SERVERNAME, "/", null);
SERVER && setCookie(SERVERNAME, SERVER, 60, "/", null, null);