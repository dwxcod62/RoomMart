(function(n) {
    function t(t, i) {
        return t.length > 0 ? parseInt(n.css(t[0], i)) || 0 : 0
    }

    function i(n) {
        return n.length > 0 ? n[0].offsetWidth + t(n, "marginLeft") + t(n, "marginRight") : 0
    }

    function r(n) {
        return n.length > 0 ? n[0].offsetHeight + t(n, "marginTop") + t(n, "marginBottom") : 0
    }
    n.fn.jCarouselLite = function(t) {
        return t = n.extend({
            btnPrev: null,
            btnNext: null,
            btnGo: null,
            mouseWheel: !1,
            auto: null,
            hoverPause: !1,
            speed: 200,
            easing: null,
            vertical: !1,
            circular: !0,
            visible: 3,
            start: 0,
            scroll: 1,
            fade: !1,
            beforeStart: null,
            afterEnd: null
        }, t || {}), this.each(function() {
            function y() {
                p();
                d = setInterval(function() {
                    c(u - t.scroll)
                }, 5e3)
            }

            function p() {
                clearInterval(d)
            }

            function g() {
                return e.slice(u).slice(0, s)
            }

            function c(i) {
                if (!w) {
                    if (t.beforeStart && t.beforeStart.call(this, g()), t.circular) i < 0 ? (f.css(a, -((u + l) * h) + "px"), u = i + l) : i > v - s ? (f.css(a, -((u - l) * h) + "px"), u = i - l) : u = i;
                    else {
                        if (i < 0 || i > v - s) return;
                        u = i
                    }
                    w = !0;
                    f.animate(a == "left" ? {
                        left: -(u * h)
                    } : {
                        top: -(u * h)
                    }, t.speed, t.easing, function() {
                        t.afterEnd && t.afterEnd.call(this, g());
                        w = !1
                    });
                    t.circular || (n(t.btnPrev + "," + t.btnNext).removeClass("disabled"), n(u - t.scroll < 0 && t.btnPrev || u + t.scroll > v - s && t.btnNext || []).addClass("disabled"))
                }
                return !1
            }
            var w = !1,
                a = t.vertical ? "top" : "left",
                k = t.vertical ? "height" : "width",
                o = n(this),
                f = n("ul", o),
                b = n("li", f),
                l = b.size(),
                s = t.visible,
                d;
            t.circular && (f.prepend(b.slice(l - s + 1).clone()).append(b.slice(0, t.scroll).clone()), t.start += s - 1);
            var e = n("li", f),
                v = e.size(),
                u = t.start;
            o.css("visibility", "visible");
            e.css({
                overflow: "hidden",
                float: t.vertical ? "none" : "left"
            });
            f.css({
                margin: "0",
                padding: "0",
                position: "relative",
                "list-style-type": "none",
                "z-index": "1"
            });
            o.css({
                overflow: "hidden",
                position: "relative",
                "z-index": "2",
                left: "0px"
            });
            var h = t.vertical ? r(e) : i(e),
                nt = h * v,
                tt = h * s;
            e.css({
                width: e.width(),
                height: e.height()
            });
            f.css(k, nt + "px").css(a, -(u * h));
            o.css(k, tt + "px");
            t.btnPrev && (n(t.btnPrev).click(function() {
                return c(u - t.scroll)
            }), t.hoverPause && n(t.btnPrev).hover(function() {
                p()
            }, function() {
                y()
            }));
            t.btnNext && (n(t.btnNext).click(function() {
                return c(u + t.scroll)
            }), t.hoverPause && n(t.btnNext).hover(function() {
                p()
            }, function() {
                y()
            }));
            t.btnGo && n.each(t.btnGo, function(i, r) {
                n(r).click(function() {
                    return c(t.circular ? t.visible + i : i)
                })
            });
            t.mouseWheel && o.mousewheel && o.mousewheel(function(n, i) {
                return i > 0 ? c(u - t.scroll) : c(u + t.scroll)
            });
            t.auto && (t.hoverPause && o.hover(function() {
                p()
            }, function() {
                y()
            }), y())
        })
    }
})(jQuery);