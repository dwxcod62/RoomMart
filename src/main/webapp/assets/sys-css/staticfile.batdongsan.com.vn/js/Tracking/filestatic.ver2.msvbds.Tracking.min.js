(function(n) {
    function u(n) {
        !n || (t = n);
        r()
    }

    function i() {
        window.trackingCallbacks = window.trackingCallbacks || []
    }

    function f(n, t) {
        i();
        n && window.trackingCallbacks.push({
            callback: n,
            id: t
        })
    }

    function r() {
        if (i(), window.trackingCallbacks.length)
            for (let n = 0; n < window.trackingCallbacks.length; n++) {
                let r = window.trackingCallbacks[n].callback,
                    i = window.trackingCallbacks[n].id;
                if (!!i && t[i] != null && !t[i]) {
                    window.enableDebugLog && console.info("tracking callback disabled", i);
                    continue
                }
                window.enableDebugLog && console.info("trigger tracking callback", i);
                r()
            }
    }
    var t = {};
    n.init = u;
    n.addTrackingCallback = f;
    n.invokeTrackingCallbacks = r
})(window.Tracking || (window.Tracking = {}));